<?php
namespace RestQuery\Action;


class AnalyzeLookUp
{



}

/*
 * NOTE
 * function PullRecordField($RecordType, $selectorType, $hintScope) {
	//assume single array is source of data
	$targetSet = $recordType . '-' $selectorType .'-' $hintScope //e.g. org-pr-all
	//call key from
	return $array[$RecordType][$targetSet];
}
//put inside lambda, insert lambda into
inside lambda...
PullRecordField( 'org', 'pr', 'all');
 *
 *
 *
 */